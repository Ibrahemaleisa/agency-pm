/**
 * Arabic brand font (Thmanyah Sans).
 *
 * Licensing: Thmanyah fonts must be downloaded from https://font.thmanyah.com and may only be
 * shipped bundled inside the product — never committed to a public repository or offered for
 * download. Once the files are in src/fonts/thmanyah/ (private repo only), load them here with
 * next/font/local using `variable: "--font-thmanyah"` and export its `.variable` class.
 * Until then, Arabic text uses IBM Plex Sans Arabic (see --font-sans in globals.css).
 */
export const arabicFontClass = "";
